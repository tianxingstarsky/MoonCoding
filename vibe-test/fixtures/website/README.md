# vibe-test fixture — TodoList project

This directory is the empty seed a session starts in. The AI will use the `vibe` CLI to materialize real code into `server.py`, `index.html`, `app.js`, `style.css`.

No other files should ever be here initially.